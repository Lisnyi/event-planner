import styled from "styled-components";
import { CustomButton } from "..";

export const Button = styled(CustomButton)`
    width: 114px;
    height: 40px;
    padding: 10px 24px;
    font-size: 14px;
    line-height: 20px;
    box-shadow: none;
`