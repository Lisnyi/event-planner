import styled from 'styled-components'
import { FormControl } from '@mui/material'

export const Box = styled(FormControl)`
    &.MuiFormControl-root {
        display: flex;
        position: static;
    }
`